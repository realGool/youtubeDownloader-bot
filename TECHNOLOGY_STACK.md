# 🛠️ Технологический стек бота

## Основные компоненты

### 1. **yt-dlp** - Загрузка видео
**Версия:** 2025.10.22+

**Что делает:**
- Скачивает видео с YouTube и других платформ
- Извлекает аудио потоки
- Обрабатывает различные форматы (DASH, HLS)
- Получает метаданные (название, длительность, превью)

**Настройки в боте:**
```python
ydl_opts = {
    'format': 'bestvideo[ext=mp4][height<=1080]+bestaudio[ext=m4a]',  # Формат видео
    'noplaylist': True,  # Только одно видео
    'skip_unavailable_fragments': True,  # Пропускать недоступные фрагменты
    'quiet': True,  # Не выводить логи
    'no_warnings': True,  # Без предупреждений
    'retries': 3,  # Количество попыток
}
```

**Для MP3:**
```python
ydl_opts.update({
    'format': 'worstaudio/worst',  # Используем самый легкий формат
    'postprocessors': [{
        'key': 'FFmpegExtractAudio',  # Конвертация через FFmpeg
        'preferredcodec': 'mp3',  # Кодек выхода
        'preferredquality': '128',  # Качество (kbps)
    }],
})
```

**Официальный сайт:** https://github.com/yt-dlp/yt-dlp

---

### 2. **FFmpeg** - Конвертация аудио/видео
**Версия:** 8.0

**Что делает:**
- Конвертирует аудио из видео в MP3
- Обрабатывает различные аудио/видео кодеки
- Экстрактирует аудио дорожки
- Оптимизирует размер файла

**Используется в боте для:**
- Конвертации видео → MP3
- Извлечения аудио из видео
- Оптимизации качества и размера

**Примеры команд, которые выполняет за кулисами:**
```bash
# Конвертация в MP3
ffmpeg -i input.mp4 -vn -acodec libmp3lame -ab 128k output.mp3

# Комбинирование видео и аудио
ffmpeg -i video.mp4 -i audio.m4a -c:v copy -c:a copy output.mp4
```

**Официальный сайт:** https://ffmpeg.org/

---

### 3. **aiogram** - Telegram Bot API
**Версия:** 3.22.0+

**Что делает:**
- Взаимодействие с Telegram Bot API
- Обработка команд и сообщений
- Управление состояниями (FSM)
- Обработка callbacks (кнопки)

**Основные компоненты:**
- `Bot` - экземпляр бота
- `Dispatcher` - диспетчер обработчиков
- `FSMContext` - управление состояниями
- `MemoryStorage` - хранение состояний в памяти

**Официальный сайт:** https://aiogram.dev/

---

### 4. **aiofiles** - Асинхронная работа с файлами
**Версия:** 24.1.0+

**Что делает:**
- Асинхронное чтение/запись файлов
- Не блокирует выполнение других операций

**В боте используется для:**
- Асинхронного открытия файлов перед отправкой

**Официальный сайт:** https://github.com/Tinche/aiofiles

---

### 5. **python-dotenv** - Управление переменными окружения
**Версия:** 1.2.1+

**Что делает:**
- Загружает переменные из .env файла
- Безопасное хранение токена бота

**Официальный сайт:** https://github.com/theskumar/python-dotenv

---

## Процесс скачивания (пошагово)

### 📹 Процесс для MP4:

1. **Получение URL** → Пользователь отправляет ссылку
2. **Выбор качества** → Пользователь выбирает low/medium/best
3. **Анализ форматов** → yt-dlp анализирует доступные форматы
4. **Скачивание** → yt-dlp скачивает видео и аудио потоки
5. **Комбинирование** → FFmpeg объединяет видео + аудио в один файл
6. **Отправка** → Отправка готового MP4 через Telegram

### 🎵 Процесс для MP3:

1. **Получение URL** → Пользователь отправляет ссылку
2. **Скачивание** → yt-dlp скачивает аудио поток
3. **Конвертация** → FFmpeg конвертирует в MP3 формат
4. **Оптимизация** → Снижение качества до 128kbps
5. **Отправка** → Отправка MP3 через Telegram

---

## Настройки производительности

### Оптимизации для скорости:

**MP3:**
- ✅ Используется `worstaudio` - самый легкий формат
- ✅ Качество 128kbps вместо 192kbps
- ✅ Быстрая конвертация

**MP4:**
- ✅ Ограничение разрешения до 1080p
- ✅ Пропуск недоступных фрагментов
- ✅ Отключены subtitles и thumbnails
- ✅ Минимальное количество попыток

**Общие:**
- ✅ Не проверяются сертификаты (быстрее)
- ✅ Отключены предупреждения
- ✅ Оптимизированы настройки сети

---

## Структура зависимостей

```
bot.py
├── aiogram (Telegram API)
│   ├── aiohttp (HTTP клиент)
│   ├── pydantic (валидация данных)
│   └── magic-filter (фильтры)
├── yt-dlp (Скачивание видео)
│   ├── requests (HTTP запросы)
│   ├── websockets (WebSocket соединения)
│   ├── mutagen (метаданные аудио)
│   └── brotli (сжатие)
└── FFmpeg (внешний бинарник)
    └── Различные кодеки
```

---

## Альтернативные решения

### yt-dlp вместо youtube-dl:
- ✅ Более быстрая работа
- ✅ Поддержка больше сайтов
- ✅ Лучшая обработка ошибок
- ✅ Регулярные обновления

### aiogram вместо python-telegram-bot:
- ✅ Асинхронный
- ✅ Современный API
- ✅ FSM из коробки
- ✅ Проще в использовании

---

## Системные требования

### Минимальные:
- Python 3.10+
- FFmpeg 4.0+
- 1 GB RAM
- 5 GB свободного места
- Интернет соединение

### Рекомендуемые:
- Python 3.12+
- FFmpeg 8.0+
- 2 GB RAM
- 10 GB свободного места
- Быстрое интернет соединение

---

## Безопасность

✅ **Безопасное хранение токена:**
- Токен хранится в .env файле
- .env не попадает в git (в .gitignore)

✅ **Очистка временных файлов:**
- Автоматическая очистка после отправки
- Временные файлы в /tmp

✅ **Ограничения:**
- Максимальный размер файла: 50MB
- Валидация URL
- Обработка ошибок

---

## Логирование

Все операции логируются:
```
INFO:aiogram.dispatcher:Start polling
INFO:aiogram.dispatcher:Run polling for bot
ERROR: Download error: [подробности]
```

Логи сохраняются в `bot.log`



